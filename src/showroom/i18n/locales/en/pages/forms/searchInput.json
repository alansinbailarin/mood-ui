{
  "title": "SearchInput",
  "description": "Search-optimised input: magnifier icon, automatic clear button, optional debounce and keyboard shortcut support.",

  "controls": {
    "variant": "Variant",
    "color": "Color",
    "size": "Size",
    "shortcut": "Shortcut",
    "loading": "Loading",
    "disabled": "Disabled"
  },

  "playground": {
    "placeholder": "Search components…"
  },

  "examples": {
    "basic": {
      "title": "Basic usage",
      "desc": "Magnifier icon and automatic ✕ button when there is a value.",
      "ph": "Search components…"
    },
    "debounce": {
      "title": "With debounce",
      "desc": "Emits the search event after the specified delay. Useful for API calls.",
      "ph": "Search with 400ms debounce"
    },
    "shortcut": {
      "title": "With shortcut",
      "desc": "Shows a kbd hint on the right. The combination focuses the input globally.",
      "ph": "Search"
    },
    "sizes": {
      "title": "Sizes",
      "desc": "Three sizes aligned with the rest of the inputs."
    },
    "loading": {
      "title": "Loading",
      "desc": "Shows a spinner instead of the clear button while searching.",
      "ph": "Searching…"
    }
  },

  "props": {
    "modelValue": "Input value (v-model).",
    "label": "Visible label above the input.",
    "placeholder": "Placeholder when empty.",
    "helperText": "Helper text. Hidden when errorText is present.",
    "errorText": "Error message that marks the input as invalid.",
    "variant": "Visual style.",
    "color": "Semantic color applied to the focus state.",
    "size": "Visual size.",
    "radius": "Corner radius. Inherits from ModoProvider when omitted.",
    "halo": "Persistent halo (ring) style.",
    "fullWidth": "Applies w-full to the root.",
    "disabled": "Disables the input.",
    "readonly": "Read-only.",
    "required": "Marks the field as required.",
    "loading": "Shows a spinner instead of the clear button.",
    "maxLength": "Maximum length. Activates the counter if showCounter.",
    "showCounter": "Shows the character counter.",
    "debounce": "Delay (ms) before emitting 'search' while typing. 0 = only emits on Enter.",
    "shortcut": "Global keyboard shortcut that focuses the input. Shown as a kbd hint (e.g. 'cmd+k').",
    "name": "HTML name attribute.",
    "id": "Input id. Auto-generated if omitted.",
    "autofocus": "Auto-focus on mount.",
    "ariaLabel": "Accessible name when there is no visible label."
  },

  "emits": {
    "updateModelValue": "Emitted on every value change (syncs v-model).",
    "change": "Emitted after a user-confirmed change.",
    "focus": "Emitted when the input receives focus.",
    "blur": "Emitted when the input loses focus.",
    "clear": "Emitted when the clear button is clicked.",
    "search": "Emitted on Enter or after the debounce delay."
  },
  "types": {
    "title": "SearchInput",
    "desc": "TypeScript interface for the SearchInput component props. Includes debounce and global keyboard shortcut support."
  },
  "a11y": {
    "kbTab": "Move focus to the next focusable element",
    "kbShiftTab": "Move focus to the previous focusable element",
    "kbType": "Type the search query",
    "kbEnter": "Trigger the search action",
    "kbEsc": "Clear the current value when clearable",
    "ariaSearch": "The input is exposed with role search for semantic context",
    "ariaInvalid": "Set to true when errorText is present",
    "ariaDescribedBy": "Links helper or error text to the input for screen reader context",
    "ariaRequired": "Set to true when the required prop is active",
    "ariaDisabled": "Communicates disabled state to assistive technologies",
    "ariaLabel": "Provides an accessible name when no visible label is present",
    "focusNative": "Uses a native input element — participates in the tab order by default",
    "focusShortcut": "Global keyboard shortcut (e.g. Cmd+K) moves focus directly to the search input"
  }
}
